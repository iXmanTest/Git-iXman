ยอดเยี่ยมเลยครับ! นี่คือโปรเจกต์ที่คุณต้องการ: **"Next.js Dashboard สำหรับร้านกาแฟ"** โดยใช้ JavaScript (`.jsx`), `src/pages`, รวม API ไว้ในโปรเจกต์เดียว, เชื่อมต่อฐานข้อมูล MSSQL, ใช้ `fetch` แทน `axios`, และตกแต่งด้วย Material-UI ให้สวยงามและดูเป็นมืออาชีพ

เราจะสร้างโปรเจกต์นี้ไปทีละขั้นตอนอย่างละเอียด พร้อมโค้ดตัวอย่างและคำอธิบายในทุกส่วนครับ

---

### ภาพรวมของโปรเจกต์
*   **Frontend:** Next.js (React) + Material-UI สำหรับหน้า Dashboard แสดงรายการกาแฟและฟอร์มเพิ่มกาแฟใหม่
*   **Backend:** Next.js API Routes ทำหน้าที่เป็น RESTful API สำหรับจัดการข้อมูลกาแฟ (GET, POST)
*   **Database:** Microsoft SQL Server (MSSQL)
*   **Deployment:** Docker และ Docker Compose เพื่อรันทั้ง Next.js และ MSSQL Server ได้ง่ายๆ ในคำสั่งเดียว

---

### สิ่งที่ต้องมีก่อนเริ่ม (Prerequisites)
1.  **Node.js:** เวอร์ชั่น 18 ขึ้นไป
2.  **Docker Desktop:** สำหรับรัน Container ของ Next.js และ MSSQL
3.  **Code Editor:** แนะนำ VS Code
4.  **โปรแกรมจัดการฐานข้อมูล (แนะนำ):** DBeaver หรือ Azure Data Studio เพื่อเข้าไปดูข้อมูลใน MSSQL ได้ง่ายๆ

---

### ขั้นตอนที่ 1: สร้างโปรเจกต์ Next.js และติดตั้ง Dependencies

1.  **สร้างโปรเจกต์ Next.js:**
    เปิด Terminal หรือ Command Prompt แล้วรันคำสั่งนี้:
    ```bash
    npx create-next-app@latest coffee-dashboard --js --eslint --tailwind --src-dir --app "No" --import-alias "@/*"
    ```
    *   `--js`: ระบุว่าเราจะใช้ JavaScript
    *   `--src-dir`: สร้างโฟลเดอร์ `src` สำหรับเก็บโค้ด
    *   `--app "No"`: เลือกใช้ `pages` Router ตามที่คุณต้องการ
    *   จากนั้นเข้าไปในโฟลเดอร์โปรเจกต์: `cd coffee-dashboard`

2.  **ติดตั้ง Dependencies ที่จำเป็น:**
    เราจะติดตั้ง Material-UI และ Driver สำหรับ MSSQL
    ```bash
    npm install @mui/material @emotion/react @emotion/styled @mui/icons-material mssql
    ```

3.  **ลบไฟล์ที่ไม่จำเป็น:**
    *   ลบไฟล์ใน `src/pages/api/hello.js`
    *   ลบเนื้อหาใน `src/styles/globals.css` ให้เหลือแค่ส่วนของ Tailwind (ถ้าคุณเลือกติดตั้ง) หรือลบทั้งหมดก็ได้ เพราะเราจะใช้ `CssBaseline` ของ MUI เป็นหลัก
    *   ลบเนื้อหาใน `src/pages/index.js` เราจะสร้างหน้า Dashboard แทน

---

### ขั้นตอนที่ 2: ตั้งค่า Docker และ Docker Compose

นี่คือหัวใจสำคัญที่จะทำให้โปรเจกต์ของเรารันได้ทุกที่!

1.  **สร้าง `Dockerfile` สำหรับ Next.js:**
    ที่ Root ของโปรเจกต์ (ระดับเดียวกับ `package.json`), สร้างไฟล์ชื่อ `Dockerfile` (ไม่มีนามสกุล) แล้วใส่โค้ดนี้:

    ```dockerfile
    # Dockerfile

    # Stage 1: Install dependencies
    FROM node:18-alpine AS deps
    WORKDIR /app
    COPY package.json package-lock.json ./
    RUN npm install

    # Stage 2: Build the application
    FROM node:18-alpine AS builder
    WORKDIR /app
    COPY --from=deps /app/node_modules ./node_modules
    COPY . .
    # Environment variables for build time
    # (ถ้ามี Public Env Vars ให้ใส่ตรงนี้)
    # ENV NEXT_PUBLIC_API_URL=...
    RUN npm run build

    # Stage 3: Production image
    FROM node:18-alpine AS runner
    WORKDIR /app
    ENV NODE_ENV=production
    COPY --from=builder /app/public ./public
    COPY --from=builder /app/.next ./.next
    COPY --from=builder /app/node_modules ./node_modules
    COPY --from=builder /app/package.json ./package.json

    EXPOSE 3000
    CMD ["npm", "start"]
    ```
    **คำอธิบาย:** เราใช้ Multi-stage build เพื่อให้ Image สุดท้ายมีขนาดเล็กและปลอดภัย
    *   `deps`: ติดตั้งเฉพาะ Dependencies
    *   `builder`: นำ Dependencies มาใช้และ Build โปรเจกต์
    *   `runner`: นำเฉพาะไฟล์ที่จำเป็นจากการ Build มาใช้งานจริง

2.  **สร้างไฟล์ `.dockerignore`:**
    เพื่อไม่ให้ Docker ก็อปปี้ไฟล์ที่ไม่จำเป็นเข้าไปใน Image สร้างไฟล์ `.dockerignore` แล้วใส่:
    ```
    .env
    .env.local
    .env.development.local
    .env.test.local
    .env.production.local

    node_modules
    .next
    npm-debug.log
    README.md
    .git
    ```

3.  **สร้างไฟล์ `docker-compose.yml`:**
    ไฟล์นี้จะทำหน้าที่สั่งรันทั้ง Next.js app และ MSSQL database ของเราพร้อมกัน
    ```yaml
    # docker-compose.yml

    version: '3.8'

    services:
      # Next.js Application
      next-app:
        container_name: coffee_dashboard_app
        build:
          context: .
          dockerfile: Dockerfile
        restart: unless-stopped
        ports:
          - "3000:3000"
        environment:
          # ส่งค่า Environment Variables ไปให้ Next.js Container
          - DB_HOST=db
          - DB_PORT=1433
          - DB_USER=sa
          - DB_PASSWORD=${DB_SA_PASSWORD} # อ่านจากไฟล์ .env
          - DB_DATABASE=CoffeeDB
        depends_on:
          - db
        networks:
          - app-network

      # MSSQL Database
      db:
        container_name: mssql_db
        image: mcr.microsoft.com/mssql/server:2019-latest
        restart: unless-stopped
        environment:
          ACCEPT_EULA: "Y"
          SA_PASSWORD: ${DB_SA_PASSWORD} # อ่านจากไฟล์ .env
        ports:
          # เปิด port 1433 ให้เชื่อมต่อจากภายนอกได้ (เช่น DBeaver)
          - "1433:1433"
        volumes:
          # เก็บข้อมูลของ database ไว้ที่ host เพื่อไม่ให้ข้อมูลหาย
          - mssql_data:/var/opt/mssql
        networks:
          - app-network

    volumes:
      mssql_data:

    networks:
      app-network:
        driver: bridge
    ```

4.  **สร้างไฟล์ `.env`:**
    ไฟล์นี้ใช้เก็บข้อมูลสำคัญ (Secrets) ที่เราจะไม่อัปโหลดขึ้น Git
    ```env
    # .env

    # รหัสผ่านสำหรับ User 'sa' ของ MSSQL (ตั้งให้ปลอดภัย!)
    DB_SA_PASSWORD=yourStrong(!)Password123
    ```
    **สำคัญ:** อย่าลืมเพิ่ม `.env` ในไฟล์ `.gitignore` ของคุณ!

---

### ขั้นตอนที่ 3: สร้าง Backend (API Routes) และเชื่อมต่อ MSSQL

1.  **สร้าง Logic การเชื่อมต่อฐานข้อมูล:**
    สร้างไฟล์ `src/lib/db.js`
    ```javascript
    // src/lib/db.js
    import sql from 'mssql';

    const dbConfig = {
        user: process.env.DB_USER,
        password: process.env.DB_PASSWORD,
        server: process.env.DB_HOST,
        database: process.env.DB_DATABASE,
        options: {
            encrypt: false, // สำหรับการรันบน Docker ภายใน network เดียวกัน
            trustServerCertificate: true, // สำหรับ local development
        },
    };

    let pool;

    export async function getConnection() {
        if (!pool) {
            try {
                pool = await sql.connect(dbConfig);
                console.log("Connected to MSSQL");
            } catch (err) {
                console.error("Database connection failed:", err);
                // ถ้าเชื่อมต่อไม่ได้ ให้ throw error เพื่อให้ API รับรู้
                throw new Error("Failed to connect to the database.");
            }
        }
        return pool;
    }
    ```

2.  **สร้าง API Endpoint (`/api/coffees`):**
    สร้างไฟล์ `src/pages/api/coffees/index.js`
    ```javascript
    // src/pages/api/coffees/index.js
    import { getConnection } from '@/lib/db';
    import sql from 'mssql';

    export default async function handler(req, res) {
        try {
            const pool = await getConnection();

            if (req.method === 'GET') {
                // ดึงข้อมูลกาแฟทั้งหมด
                const result = await pool.request().query('SELECT * FROM Coffees ORDER BY Id DESC');
                res.status(200).json(result.recordset);

            } else if (req.method === 'POST') {
                // เพิ่มกาแฟใหม่
                const { name, origin, price, imageUrl } = req.body;

                if (!name || !origin || !price) {
                    return res.status(400).json({ message: 'Name, origin, and price are required.' });
                }

                const result = await pool.request()
                    .input('name', sql.NVarChar, name)
                    .input('origin', sql.NVarChar, origin)
                    .input('price', sql.Decimal(10, 2), price)
                    .input('imageUrl', sql.NVarChar, imageUrl || 'https://images.unsplash.com/photo-1511920183353-3c9c13aaa489?auto=format&fit=crop&q=80&w=250') // Default image
                    .query('INSERT INTO Coffees (Name, Origin, Price, ImageUrl) VALUES (@name, @origin, @price, @imageUrl)');

                res.status(201).json({ message: 'Coffee added successfully!', data: req.body });

            } else {
                res.setHeader('Allow', ['GET', 'POST']);
                res.status(405).end(`Method ${req.method} Not Allowed`);
            }

        } catch (error) {
            console.error('API Error:', error);
            res.status(500).json({ message: 'Internal Server Error', error: error.message });
        }
    }
    ```
    **คำอธิบาย:**
    *   เราใช้ `getConnection()` เพื่อเชื่อมต่อ DB
    *   ใช้ `req.method` เพื่อแยกว่าจะทำงาน GET หรือ POST
    *   **GET:** ดึงข้อมูลทั้งหมดจากตาราง `Coffees`
    *   **POST:** รับข้อมูลจาก `req.body` แล้ว `INSERT` ลงตาราง
    *   **สำคัญ:** เราใช้ `.input()` เพื่อทำ Parameterized Query ซึ่งช่วยป้องกัน **SQL Injection** ได้อย่างมีประสิทธิภาพ

---

### ขั้นตอนที่ 4: สร้าง Frontend (Dashboard) ด้วย Material-UI

1.  **ตั้งค่า Theme และ Provider ของ Material-UI:**
    สร้างไฟล์ `src/styles/theme.js` เพื่อกำหนด Theme ของเรา
    ```javascript
    // src/styles/theme.js
    import { createTheme } from '@mui/material/styles';

    const theme = createTheme({
        palette: {
            mode: 'light', // หรือ 'dark'
            primary: {
                main: '#6F4E37', // Coffee Brown
            },
            secondary: {
                main: '#A0522D', // Sienna
            },
            background: {
                default: '#F5F5F5', // Light Gray
                paper: '#FFFFFF',
            },
        },
        typography: {
            fontFamily: '"Roboto", "Helvetica", "Arial", sans-serif',
            h4: {
                fontWeight: 700,
                color: '#4A2C2A', // Darker Brown
            },
            h5: {
                fontWeight: 600,
            },
        },
        components: {
            MuiButton: {
                styleOverrides: {
                    root: {
                        borderRadius: 8,
                        textTransform: 'none',
                        padding: '10px 20px',
                    },
                },
            },
            MuiCard: {
                styleOverrides: {
                    root: {
                        borderRadius: 12,
                        boxShadow: '0 4px 12px rgba(0,0,0,0.08)',
                    },
                },
            },
        },
    });

    export default theme;
    ```

2.  **นำ Theme ไปใช้ใน `_app.js`:**
    แก้ไขไฟล์ `src/pages/_app.js`
    ```javascript
    // src/pages/_app.js
    import { ThemeProvider } from '@mui/material/styles';
    import CssBaseline from '@mui/material/CssBaseline';
    import theme from '@/styles/theme';
    import '@/styles/globals.css'; // หากยังต้องการใช้

    export default function App({ Component, pageProps }) {
      return (
        <ThemeProvider theme={theme}>
          {/* CssBaseline คือตัวช่วย reset CSS ให้ทำงานกับ MUI ได้ดี */}
          <CssBaseline />
          <Component {...pageProps} />
        </ThemeProvider>
      );
    }
    ```

3.  **สร้างหน้า Dashboard หลัก:**
    สร้างโฟลเดอร์ `src/pages/dashboard` และสร้างไฟล์ `coffee.jsx` ข้างใน
    **`src/pages/dashboard/coffee.jsx`**

    ```jsx
    import { useState, useEffect } from 'react';
    import Head from 'next/head';
    import {
        Container, Typography, Grid, Card, CardContent, CardMedia,
        TextField, Button, Box, CircularProgress, Alert
    } from '@mui/material';
    import AddCircleOutlineIcon from '@mui/icons-material/AddCircleOutline';

    // Component สำหรับแสดง Card กาแฟแต่ละใบ
    const CoffeeCard = ({ coffee }) => (
        <Card sx={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
            <CardMedia
                component="img"
                height="180"
                image={coffee.ImageUrl || 'https://via.placeholder.com/300x180.png?text=Coffee'}
                alt={coffee.Name}
            />
            <CardContent sx={{ flexGrow: 1 }}>
                <Typography gutterBottom variant="h5" component="div">
                    {coffee.Name}
                </Typography>
                <Typography variant="body2" color="text.secondary">
                    Origin: {coffee.Origin}
                </Typography>
                <Typography variant="h6" color="primary" sx={{ mt: 2 }}>
                    ${parseFloat(coffee.Price).toFixed(2)}
                </Typography>
            </CardContent>
        </Card>
    );

    // Component ฟอร์มสำหรับเพิ่มกาแฟ
    const AddCoffeeForm = ({ onAddCoffee }) => {
        const [name, setName] = useState('');
        const [origin, setOrigin] = useState('');
        const [price, setPrice] = useState('');

        const handleSubmit = (e) => {
            e.preventDefault();
            if (!name || !origin || !price) {
                alert('Please fill all fields');
                return;
            }
            onAddCoffee({ name, origin, price: parseFloat(price) });
            // Reset form
            setName('');
            setOrigin('');
            setPrice('');
        };

        return (
            <Card sx={{ p: 3, mb: 4 }}>
                <Typography variant="h5" gutterBottom>Add New Coffee</Typography>
                <Box component="form" onSubmit={handleSubmit} noValidate sx={{ mt: 1 }}>
                    <Grid container spacing={2}>
                        <Grid item xs={12} sm={6}>
                            <TextField
                                fullWidth
                                label="Coffee Name"
                                value={name}
                                onChange={(e) => setName(e.target.value)}
                                required
                            />
                        </Grid>
                        <Grid item xs={12} sm={6}>
                            <TextField
                                fullWidth
                                label="Origin"
                                value={origin}
                                onChange={(e) => setOrigin(e.target.value)}
                                required
                            />
                        </Grid>
                        <Grid item xs={12}>
                            <TextField
                                fullWidth
                                label="Price"
                                type="number"
                                value={price}
                                onChange={(e) => setPrice(e.target.value)}
                                required
                            />
                        </Grid>
                    </Grid>
                    <Button
                        type="submit"
                        variant="contained"
                        startIcon={<AddCircleOutlineIcon />}
                        sx={{ mt: 3 }}
                    >
                        Add Coffee
                    </Button>
                </Box>
            </Card>
        );
    };


    export default function CoffeeDashboard() {
        const [coffees, setCoffees] = useState([]);
        const [loading, setLoading] = useState(true);
        const [error, setError] = useState(null);

        // ฟังก์ชันสำหรับดึงข้อมูลกาแฟ
        const fetchCoffees = async () => {
            setLoading(true);
            setError(null);
            try {
                // ใช้ Fetch API ตามที่คุณต้องการ
                const response = await fetch('/api/coffees');
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
                const data = await response.json();
                setCoffees(data);
            } catch (e) {
                console.error("Failed to fetch coffees:", e);
                setError('Could not load coffee data. Please try again later.');
            } finally {
                setLoading(false);
            }
        };

        // ดึงข้อมูลเมื่อ Component โหลดครั้งแรก
        useEffect(() => {
            fetchCoffees();
        }, []);

        // ฟังก์ชันสำหรับจัดการการเพิ่มกาแฟ
        const handleAddCoffee = async (newCoffee) => {
            try {
                const response = await fetch('/api/coffees', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(newCoffee),
                });
                if (!response.ok) {
                    throw new Error('Failed to add coffee');
                }
                // ดึงข้อมูลใหม่เพื่ออัปเดต UI
                fetchCoffees();
            } catch (e) {
                console.error("Failed to add coffee:", e);
                setError('Could not add coffee. Please try again.');
            }
        };

        return (
            <>
                <Head>
                    <title>Coffee Dashboard</title>
                    <meta name="description" content="Manage your coffee inventory" />
                </Head>
                <Container maxWidth="lg" sx={{ py: 4 }}>
                    <Typography variant="h4" component="h1" gutterBottom>
                        Coffee Inventory Dashboard
                    </Typography>

                    <AddCoffeeForm onAddCoffee={handleAddCoffee} />

                    {error && <Alert severity="error" sx={{ mb: 2 }}>{error}</Alert>}

                    {loading ? (
                        <Box sx={{ display: 'flex', justifyContent: 'center', my: 4 }}>
                            <CircularProgress />
                        </Box>
                    ) : (
                        <Grid container spacing={4}>
                            {coffees.map((coffee) => (
                                <Grid item key={coffee.Id} xs={12} sm={6} md={4}>
                                    <CoffeeCard coffee={coffee} />
                                </Grid>
                            ))}
                        </Grid>
                    )}
                </Container>
            </>
        );
    }
    ```

4.  **(ตัวเลือก) Redirect หน้าแรกไปที่ Dashboard:**
    แก้ไขไฟล์ `src/pages/index.js` ให้ Redirect ไปที่ `/dashboard/coffee`
    ```jsx
    // src/pages/index.js
    import { useEffect } from 'react';
    import { useRouter } from 'next/router';

    export default function HomePage() {
      const router = useRouter();

      useEffect(() => {
        router.replace('/dashboard/coffee');
      }, [router]);

      return (
        <div>Redirecting...</div>
      );
    }
    ```

---

### ขั้นตอนที่ 5: รันโปรเจกต์และเตรียมฐานข้อมูล

1.  **รัน Docker Compose:**
    เปิด Terminal ที่ Root ของโปรเจกต์ แล้วรันคำสั่ง:
    ```bash
    docker-compose up --build
    ```
    *   `--build`: จะสั่งให้ Docker สร้าง Image ใหม่จาก `Dockerfile` ของเรา
    *   รอสักครู่... Docker จะดาวน์โหลด Image ของ MSSQL และ Build Image ของ Next.js จนเสร็จ
    *   เมื่อทุกอย่างเรียบร้อย คุณจะเห็น Log การทำงานของทั้ง `next-app` และ `db`

2.  **สร้างตารางใน MSSQL:**
    ตอนนี้ Database ของเรารันแล้ว แต่ยังเป็นฐานข้อมูลว่างๆ เราต้องสร้าง Table `Coffees`
    *   เปิดโปรแกรมจัดการฐานข้อมูล (DBeaver หรือ Azure Data Studio)
    *   สร้าง Connection ใหม่ไปยัง MSSQL
        *   **Host/Server:** `localhost`
        *   **Port:** `1433`
        *   **User:** `sa`
        *   **Password:** `yourStrong(!)Password123` (รหัสที่คุณตั้งในไฟล์ `.env`)
        *   **Database:** `master` (เพื่อเชื่อมต่อครั้งแรก)
    *   เมื่อเชื่อมต่อได้แล้ว ให้เปิด SQL Editor แล้วรัน Script นี้เพื่อสร้าง Database และ Table ของเรา:
    ```sql
    -- สร้าง Database ใหม่ (ถ้ายังไม่มี)
    IF NOT EXISTS (SELECT * FROM sys.databases WHERE name = 'CoffeeDB')
    BEGIN
        CREATE DATABASE CoffeeDB;
    END;
    GO

    -- สลับไปใช้ Database ที่เราสร้าง
    USE CoffeeDB;
    GO

    -- สร้างตาราง Coffees
    IF NOT EXISTS (SELECT * FROM sysobjects WHERE name='Coffees' and xtype='U')
    BEGIN
        CREATE TABLE Coffees (
            Id INT PRIMARY KEY IDENTITY(1,1),
            Name NVARCHAR(100) NOT NULL,
            Origin NVARCHAR(100) NOT NULL,
            Price DECIMAL(10, 2) NOT NULL,
            ImageUrl NVARCHAR(255),
            CreatedAt DATETIME DEFAULT GETDATE()
        );
    END;
    GO

    -- (Optional) เพิ่มข้อมูลเริ่มต้น
    INSERT INTO Coffees (Name, Origin, Price, ImageUrl) VALUES
    ('Espresso Roast', 'Latin America', 12.50, 'https://images.unsplash.com/photo-1559496417-e7f25cb247f3'),
    ('Pike Place Roast', 'Worldwide', 14.00, 'https://images.unsplash.com/photo-1497935586351-b67a49e012bf'),
    ('Ethiopia Yirgacheffe', 'Ethiopia', 18.75, 'https://images.unsplash.com/photo-1509042239860-f550ce710b93');
    GO
    ```

3.  **เข้าชมโปรเจกต์ของคุณ:**
    เปิด Browser แล้วไปที่ **http://localhost:3000**
    คุณจะถูก Redirect ไปที่หน้า Dashboard และเห็นรายการกาแฟที่คุณเพิ่งเพิ่มเข้าไป พร้อมฟอร์มสำหรับเพิ่มข้อมูลใหม่!

---

### สรุปและขั้นตอนต่อไป
ยินดีด้วยครับ! ตอนนี้คุณมีโปรเจกต์ Next.js ที่ทำงานร่วมกับ MSSQL ผ่าน Docker อย่างสมบูรณ์แล้ว โปรเจกต์นี้มีทั้ง Backend และ Frontend ในที่เดียว พร้อมดีไซน์ที่ดูสะอาดและเป็นมืออาชีพจาก Material-UI

**สิ่งที่สามารถทำต่อได้:**
*   **เพิ่มฟังก์ชัน Edit/Delete:** สร้าง API Endpoint สำหรับ `PUT` และ `DELETE` แล้วเพิ่มปุ่มใน `CoffeeCard`
*   **การยืนยันตัวตน (Authentication):** เพิ่มระบบ Login/Register (เช่นใช้ Next-Auth.js)
*   **การจัดการ State ที่ซับซ้อนขึ้น:** หากโปรเจกต์ใหญ่ขึ้น อาจพิจารณาใช้ Zustand หรือ Redux Toolkit
*   **การทำ Pagination:** หากมีข้อมูลกาแฟจำนวนมาก ควรแบ่งหน้าเพื่อประสิทธิภาพที่ดีขึ้น
*   **การ Validation ข้อมูล:** เพิ่มการตรวจสอบข้อมูลทั้งฝั่ง Frontend และ Backend ให้ละเอียดขึ้น (เช่นใช้ Zod หรือ Yup)